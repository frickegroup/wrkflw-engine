import {strict as assert } from 'node:assert'
import { before, describe as suite, it as test } from 'node:test'

suite('AMQP', () => {
	// test('should generate valid nanoid', async () => {
	// 	const nanoid = await import('../dist/index.js').then((m) => m.nanoid)
	// 	assert.match(nanoid(), /^[6-9B-DF-HJ-NP-RTW]{8}$/)
	// })
})
